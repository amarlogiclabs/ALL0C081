spring:
  application:
    name: user-service
  
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/codeverse?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    username: root
    password: root254
    driver-class-name: com.mysql.cj.jdbc.Driver
  
  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: false
    properties:
      hibernate:
        # dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

server:
  port: 8090
  servlet:
    context-path: /
  
logging:
  level:
    root: INFO
    com.codearena: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"
